import { expect, test } from '@playwright/test';

// Scaffold placeholder. Replace with real E2E flows once `pnpm --filter web dev` is wired
// to a known port and routes are stable. See Issue #4 acceptance criteria.
test('placeholder: playwright runner boots', () => {
  expect(1 + 1).toBe(2);
});
